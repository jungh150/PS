#include <iostream>
#include <vector>
using namespace std;

int k, v, e;
vector<vector<int>> g;
vector<bool> visited;
vector<int> color;
bool isBipartite;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> k;
    while (k--) {
        cin >> v >> e;
    }
}