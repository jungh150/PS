#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    
    while (t--) {
        int n;
        int bn = 2 * n;

        vector<int> b(bn);
        for (int i = 0; i < bn; i++) cin >> b[i];
    }
}